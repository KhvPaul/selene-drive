# Selene Drive

### Local setup

* Prepare env variables `cp .env.local .env` and set values if needed
* Run `docker-compose build` or  `docker-compose build --no-cache` to build
* Then `docker-compose up` to run server
